package pfq.pakingserver;

public class Scheduler {
    // private Logger logger = PFQloger.getLogger(CargoService.class,
    // Level.ALL);
    /*
     * @Scheduled(fixedRate = 60000) public void clearTempFolderRate() {
     * logger.info("I am a schedule-method with fixed rate param.");
     * logger.info("Executes every 1 minute."); }
     */
    /*
     * @Scheduled(fixedDelay = 6000000) public void clearTempFolderDelay() {
     * logger.info("I am a schedule-method with fixed delay param.");
     * logger.info("Executes after 1 minute when my previous execution ends.");
     * }
     * 
     * @Scheduled(cron = "0 0 4 * * ?") public void clearTempFolderCron() {
     * logger.info("I am a schedule-method with cron param."); logger.info(
     * "Executes at 4:00 am everyday."); }
     */
}